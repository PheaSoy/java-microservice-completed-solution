server:
  port: 8081
  servlet:
    context-path: /configure-server
spring:
  application:
    name: configure-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PheaSoy/spring-completed-microservice
          search-paths: config/{application}
management:
  endpoints:
    web:
      exposure:
        include: '*'